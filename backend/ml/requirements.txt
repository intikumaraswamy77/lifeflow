# Minimal requirements for Python 3.14 compatibility
# If this still fails, Python 3.14 is not supported for ML workloads
joblib
Pillow

# Alternative: Move to Railway, Heroku, or AWS for Python 3.11 support
